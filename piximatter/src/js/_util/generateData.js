/*
const recipe = [
  { 
    id: NO,
    type: 'circle'
  }
]
*/
export const generateData = (recipe) => {
  // circle count
  // polygon count
  // rectangle count
  // vertices count (per type)
  // vertices choice array
}

// should element position be a percentage? due to resize?

// 